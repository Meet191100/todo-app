// Configuration variables for the application
export const config = {
    // MongoDB URL for database connection
    mongoDBUrl: 'mongodb://127.0.0.1:27017/todoapp',

    // Port on which the server will run
    serverPort: 3000,
  };